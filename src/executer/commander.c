#include "executer.h"

/* 
	if (fdp->i == 0)
	{
        fprintf(stderr, "entro en first\n");
		first_cmnd(fdp, shell->cmnd_list, shell, fdp->paths);
	}
	else if (fdp->cmnd_cnt >= 3 && fdp->i + 1 != fdp->cmnd_cnt)
	{
		fprintf(stderr, "entro en middle\n");
		middle_cmnd(fdp, shell->cmnd_list, shell, fdp->paths);
	}
	else if (fdp->i > 0 && fdp->i + 1 == fdp->cmnd_cnt)
	{
        fprintf(stderr, "entro en final\n");
		final_cmnd(fdp, shell->cmnd_list, shell, fdp->paths);
	} */



